

}?xml version="1.0" encoding="utf-8"?>
    droidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns: app="http://schemas.android.com/apk/res-auto"
    xmlns: tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android: Layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

<androidx.fragment.app.FragmentContainerView
    android:id="@+id/fragmentContainerView"
    android:name="androidx.navigation. fragment. NavHostFragment"
    android:layout_width="match_parent"
    android: Layout_height="match_parent"
    app:layout_constraintBottom_toBottomOf="@+id/main"
    app: Layout_constraintEnd_toEnd0f="@+id/main"
    app:layout_constraintTop_toTopOf="@+id/main"
    app:layout_constraintStart_toStart0f="@+id/main"
    app:navGraph="@navigation/nav_onboarding"/>

    </androidx.constraintlayout. widget. ConstraintLayout>

